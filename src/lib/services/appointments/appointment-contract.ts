import {z} from "zod";
import {isoDateTimeSchema,membershipIdSchema,stableIdSchema} from "@/lib/listing/list-contract";
const fields=z.object({operationKey:stableIdSchema,calendarRevision:z.number().int().nonnegative(),subject:z.string().trim().min(1).max(300),description:z.string().trim().max(10_000).nullable().optional(),startsAt:isoDateTimeSchema,endsAt:isoDateTimeSchema.optional(),serviceVariantId:stableIdSchema.nullable().optional(),contactId:stableIdSchema.nullable().optional(),companyId:stableIdSchema.nullable().optional(),organizerMembershipId:membershipIdSchema.optional(),participantMembershipIds:z.array(membershipIdSchema).max(50).default([]),reminderEnabled:z.boolean().default(true),reminderOffsetMinutes:z.number().int().min(0).max(525600).default(15),acknowledgeConflict:z.boolean().default(false)}).strict();
export const appointmentCreateInputSchema=fields.superRefine((v,ctx)=>{if(!v.endsAt&&!v.serviceVariantId)ctx.addIssue({code:"custom",path:["endsAt"],message:"Choose an end time or service duration"});if(v.endsAt&&Date.parse(v.endsAt)<=Date.parse(v.startsAt))ctx.addIssue({code:"custom",path:["endsAt"],message:"End time must be after start time"});});
export const appointmentListInputSchema=z.object({from:isoDateTimeSchema.optional(),to:isoDateTimeSchema.optional(),scope:z.enum(["mine","all"]).default("mine"),limit:z.coerce.number().int().min(1).max(200).default(100)}).strict();
const operation=z.object({operationKey:stableIdSchema,expectedRevision:z.number().int().nonnegative()});
export const appointmentCommandInputSchema=z.discriminatedUnion("action",[operation.extend({action:z.literal("cancel"),reason:z.string().trim().min(1).max(500)}).strict(),operation.extend({action:z.literal("complete")}).strict(),operation.extend({action:z.literal("reschedule"),startsAt:isoDateTimeSchema,endsAt:isoDateTimeSchema,participantMembershipIds:z.array(membershipIdSchema).max(50),acknowledgeConflict:z.boolean().default(false)}).strict().refine(v=>Date.parse(v.endsAt)>Date.parse(v.startsAt),{path:["endsAt"],message:"End time must be after start time"})]);
export const appointmentRowSchema=z.object({id:stableIdSchema,subject:z.string(),description:z.string().nullable(),startsAt:isoDateTimeSchema,endsAt:isoDateTimeSchema,timeZone:z.string(),contactId:stableIdSchema.nullable(),companyId:stableIdSchema.nullable(),serviceVariantId:stableIdSchema.nullable(),organizerMembershipId:z.string(),participantMembershipIds:z.array(z.string()),status:z.enum(["scheduled","completed","cancelled"]),reminderEnabled:z.boolean(),reminderOffsetMinutes:z.number().int(),conflictAcknowledgedAt:isoDateTimeSchema.nullable(),revision:z.number().int(),createdAt:isoDateTimeSchema,updatedAt:isoDateTimeSchema});
export const appointmentListOutputSchema=z.object({rows:z.array(appointmentRowSchema)});
export type AppointmentCreateInput=z.infer<typeof appointmentCreateInputSchema>;
export type AppointmentListInput=z.infer<typeof appointmentListInputSchema>;
export type AppointmentCommandInput=z.infer<typeof appointmentCommandInputSchema>;
