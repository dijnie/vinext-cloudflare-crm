import {notFound} from "next/navigation";import {OperationsSettings} from "@/components/app/settings/operations-settings";import {getPageContext} from "@/lib/http/page-context";import {isAppLocale} from "@/lib/i18n/config";
export default async function OperationsPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isAppLocale(locale))notFound();const{root,context}=await getPageContext();if(context.role!=="owner")notFound();const[initialWorkspace,initialDashboard,initialWebforms]=await Promise.all([root.workspace.get(context),root.integrations.dashboard(context),root.webforms.list(context)]);return <OperationsSettings locale={locale} initialWorkspace={initialWorkspace as never} initialDashboard={initialDashboard as never} initialWebforms={initialWebforms as never} membershipId={context.membershipId}/>;}
