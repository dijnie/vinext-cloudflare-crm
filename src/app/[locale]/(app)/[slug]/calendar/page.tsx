import {notFound} from "next/navigation";import {CalendarBoard} from "@/components/app/scheduling/calendar-board";import {getPageContext} from "@/lib/http/page-context";import {isAppLocale} from "@/lib/i18n/config";
export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isAppLocale(locale))notFound();const {root,context}=await getPageContext(),settings=await root.settings.get(context);return <CalendarBoard locale={locale} timeZone={settings.timeZone} calendarRevision={settings.revision} initialData={await root.appointments.list(context,{scope:"mine",limit:100})}/>;}
