import {env} from "cloudflare:workers";
import {createCompositionRoot,type CompositionRoot,type RuntimeEnv} from "@/lib/composition-root";
import {HttpError,isHttpError} from "@/lib/http/http-errors";
import {applySecurityHeaders} from "@/lib/http/security-headers";
import {webformSubmissionSchema} from "@/lib/services/webforms/webform-contract";
function response(body:unknown,status=200){const headers=new Headers({"cache-control":"no-store","content-type":"application/json"});applySecurityHeaders(headers);return new Response(JSON.stringify(body),{status,headers});}
export function createPublicWebformHandler(root:CompositionRoot,slug:string){return async(request:Request)=>{const requestId=request.headers.get("cf-ray")??crypto.randomUUID();try{const declared=Number(request.headers.get("content-length")??0);if(declared>65_536)throw new HttpError(413,"payload_too_large","Payload is too large");const raw=await request.text();if(new TextEncoder().encode(raw).byteLength>65_536)throw new HttpError(413,"payload_too_large","Payload is too large");let json:unknown;try{json=JSON.parse(raw);}catch{throw new HttpError(400,"invalid_json","Body must be valid JSON");}const payload=webformSubmissionSchema.parse(json);return response(await root.webforms.submit(slug,payload,request.headers,raw));}catch(error){const status=isHttpError(error)?error.status:error instanceof Error&&error.name==="ZodError"?400:500,code=isHttpError(error)?error.code:status===400?"validation_failed":"internal_error";root.securityLogger({code,requestId,method:request.method,outcome:status<500?"rejected":"failed"});return response({error:{code,requestId}},status);}};}
export async function POST(request:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params;return createPublicWebformHandler(createCompositionRoot(env as RuntimeEnv),slug)(request);}
