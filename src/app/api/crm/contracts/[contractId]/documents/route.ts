import {env} from "cloudflare:workers";import {createCompositionRoot,type CompositionRoot,type RuntimeEnv} from "@/lib/composition-root";import {createRouteHandler} from "@/lib/http/route-handler";import {z} from "zod";const output=z.object({id:z.uuid(),name:z.string(),size:z.number(),uploadedAt:z.string()});export const createContractDocumentPostHandler=(root:CompositionRoot,id:string)=>createRouteHandler(root,{rawBody:true,output,unsafe:true,handle:({context,request})=>root.contractDocuments.upload(context,id,request)});export async function POST(r:Request,{params}:{params:Promise<{contractId:string}>}){return createContractDocumentPostHandler(createCompositionRoot(env as RuntimeEnv),(await params).contractId)(r)}
