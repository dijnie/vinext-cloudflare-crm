import {env} from "cloudflare:workers";
import {createCompositionRoot,type CompositionRoot,type RuntimeEnv} from "@/lib/composition-root";
import {HttpError,isHttpError} from "@/lib/http/http-errors";
import {applySecurityHeaders} from "@/lib/http/security-headers";
import {inboundEventSchema} from "@/lib/services/integrations/integration-contract";
function response(body:unknown,status=200){const headers=new Headers({"content-type":"application/json","cache-control":"no-store"});applySecurityHeaders(headers);return new Response(JSON.stringify(body),{status,headers});}
export const createIntegrationEventHandler=(root:CompositionRoot)=>async(request:Request)=>{const requestId=request.headers.get("cf-ray")??crypto.randomUUID();try{const token=(request.headers.get("authorization")??"").replace(/^Bearer\s+/,"");if(!token)throw new HttpError(401,"authentication_required","App token is required");const declared=Number(request.headers.get("content-length")??0);if(declared>262_144)throw new HttpError(413,"payload_too_large","Payload is too large");const raw=await request.text();if(new TextEncoder().encode(raw).byteLength>262_144)throw new HttpError(413,"payload_too_large","Payload is too large");let parsed:unknown;try{parsed=JSON.parse(raw);}catch{throw new HttpError(400,"invalid_json","Body must be JSON");}return response(await root.integrations.receive(token,inboundEventSchema.parse(parsed)));}catch(error){const status=isHttpError(error)?error.status:error instanceof Error&&error.name==="ZodError"?400:500,code=isHttpError(error)?error.code:status===400?"validation_failed":"internal_error";root.securityLogger({code,requestId,method:request.method,outcome:status<500?"rejected":"failed"});return response({error:{code,requestId}},status);}};
export function POST(request:Request){return createIntegrationEventHandler(createCompositionRoot(env as RuntimeEnv))(request);}
